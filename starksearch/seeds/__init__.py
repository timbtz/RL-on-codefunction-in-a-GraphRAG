"""Candidate `search(q, G)` seed programs for the STaRK target.

These are loaded as SOURCE TEXT (`SearchProgram.from_file`), not imported as
modules -- the optimizer mutates them as a program artifact.
"""
