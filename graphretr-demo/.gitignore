# Secrets — never commit
.env
.env.*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Logs & PID files
*.log
*.pid

# MLflow tracking (local DB + run artifacts)
mlflow.db
mlartifacts/
mlruns/

# Run outputs, caches, scratch
runs/
emb_cache.npy
emb_cache.meta.json
extract_cache.json
