# Strategy family "extract_first" -- the Phase-2 principled arm.
family: extract_first
seed: ../starksearch/seeds/extract_first.py
notes: >
  Query understanding via the fixed-prompt cached G.extract (LLM picks the
  answer type and likely relations from the live schema allowlists), then
  typed dense retrieval + relation-guided 1-hop expansion. Deliberately NOT
  warm-started from run1/run2 programs: their hardcoded keyword tables are the
  overfitting mode this family exists to replace.
