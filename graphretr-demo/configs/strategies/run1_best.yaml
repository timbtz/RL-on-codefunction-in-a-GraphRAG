# Strategy family "run1_best" -- warm-start continuation of campaign run1.
family: vector_only
seed: src/graphretr_opt/artifact/seeds/run1_best.py
notes: >
  Verbatim copy of runs/run1/best_search.py (gate recall@20 0.3662, accepted
  at step 14 before a claude-CLI rate-limit outage burned steps 16-29).
  Descends from the vector_only arm; same family so the archive lineage is
  continuous. Gate (size 200, seed 42) is identical to run1, so scores are
  directly comparable.
